"""Workflow Orchestration Queue (OS-APOW).

Distributed task queue using GitHub Issues as backing store with
two services: Notifier (webhook receiver) and Sentinel (background processor).
"""

__version__ = "0.1.0"
